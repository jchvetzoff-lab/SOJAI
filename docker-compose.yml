services:
  web:
    build:
      context: .
      target: deps
    env_file:
      - path: .env.local
        required: false
    environment:
      NODE_ENV: development
      PORT: 3000
    command: npm run dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
